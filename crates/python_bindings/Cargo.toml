[package]
name = "python_bindings"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
chithi-core = { workspace = true }

[dev-dependencies]
chithi-core = { workspace = true, features = ["rayon"] }
