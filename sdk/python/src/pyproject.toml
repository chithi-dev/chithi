[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chithi-sdk-py"
version = "0.1.0"
description = "Chithi SDK Python wrapper"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["chithi*"]
